apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: financetracker
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: financetracker-prod-strip-prefix@kubernetescrd
spec:
  rules:
    - host: chrispicloud.dev
      http:
        paths:
          - path: /financetracker/api
            pathType: Prefix
            backend:
              service:
                name: api
                port:
                  number: 8080
          - path: /financetracker
            pathType: Prefix
            backend:
              service:
                name: web
                port:
                  number: 80
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-prefix
spec:
  stripPrefix:
    prefixes:
      - /financetracker/api
