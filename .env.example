# --- Postgres ---
POSTGRES_USER=ft_dbadmin
POSTGRES_PASSWORD=CHANGEME
POSTGRES_DB=financedb_dev

# --- pgAdmin ---
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=CHANGEME

# --- API Connection (Container→DB via Docker DNS) ---
ConnectionStrings__DefaultConnection=Host=db;Port=5432;Database=financedb_dev;Username=ft_dbadmin;Password=CHANGEME
